# FULL TOOL Telegram Bot

Button-menu Telegram bot wrapping check / auto / ref / sex modules.

## Run local
```
pip install -r requirements.txt
python bot.py
```

## Deploy Railway
- New project → Deploy from repo
- Add env vars (optional): `BOT_TOKEN`, `ADMIN_IDS` (comma-separated)
- Worker dyno auto-detected from Procfile
